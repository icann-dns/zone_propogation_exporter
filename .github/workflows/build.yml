name: Build and Publish Package

on:
  push:
    tags:
      - "v*" # Trigger on version tags like v0.1.0, v1.2.3, etc.

jobs:
  build-and-publish:
    runs-on: ubuntu-20.04 # Ubuntu 20.04 has Python 3.6 support

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4 # v4 is the last version supporting Python 3.6
        with:
          python-version: "3.6"

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install build

      - name: Build package
        run: |
          python -m build

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Uncomment below to publish to PyPI
      # - name: Publish to PyPI
      #   env:
      #     TWINE_USERNAME: __token__
      #     TWINE_PASSWORD: ${{ secrets.PYPI_TOKEN }}
      #   run: |
      #     pip install twine
      #     twine upload dist/*
